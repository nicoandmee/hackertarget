const client = require('shodan-client');

function directIPScan() {
    return [];
}

function scanByFilters {
    // do scanning
    return [].concat('lol');
}

module.exports = {
    scanByFilters,
    directIPScan,
};
